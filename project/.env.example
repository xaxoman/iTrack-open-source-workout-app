# Optional: enable cloud sync with Supabase.
# Leave these blank to keep using local (JSON / localStorage) storage only.
#
# 1. Create a free project at https://supabase.com
# 2. In the SQL editor, run the script in supabase/schema.sql
# 3. Copy your project URL and anon/public key from Project Settings > API
# 4. Copy this file to .env and fill in the values below
#
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
